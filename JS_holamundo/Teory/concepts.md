
Expressions -> asignaciones, evaluaciones en un valor, llamados de funciones, todo esto es una exprecion (Cada vez que el codigo hace algo y se puede retornar algo)

Declaration -> Cuando se crea algo que se puede referenciar despues (let, const, (*)function, (*)async, class, export e import)

Statements -> if. else, for